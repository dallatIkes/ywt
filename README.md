# ywt
A simple app to share cool videos with your friends

## How to run ?

**First install the requirements**
```bash
pip install -r requirements.txt
```
**Then launch the `run_server.py` file**
```bash
python3 run_server.py
```
