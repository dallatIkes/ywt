# Devlog #1 - 2026-01-04

## What I did

- Created a **venv** for the project (w/ fastapi, sqlalchemy, pydantic, bcrypt)
- Created a fast app with a hello world endpoint
- Created the `database.py` file w/ the **object representation** of the tables
- Created the `models.py` file for **Data validation**
- Created 2 routers with de **CRUD operations** for both users and recos
- Quickly tested the endpooints with **Swagger**

## What I learned

- `Mapped[int]` -> Python type  
  `mapped_column(...)` ->  SQL def
- relational -> ForeiggnKey  
  object -> `relationship(...)` -> `back_populates` -> syncs both ends  
  **Real advantage of SQLAlchemy as an ORM not just a SQL mapper**
- We create a session to communicate with the database and then we close it

## What's next

- make a `auth.py` file for authentification (`/login`, `/token`, `/me`)